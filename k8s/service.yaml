apiVersion: v1
kind: Service
metadata:
  name: eval-agent-svc
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: eval-agent
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
